# Maintainer: David Torrez Reyes <davidtorrezreyes@gmail.com>
pkgname=arch-base
pkgver=1.0.0
pkgrel=1
pkgdesc='Metapaquete de mi sistema base de ArchLinux'
url='https://github.com/medicendav/arch-base'
arch=('any')
license=('MIT')
depends=(
    # base
    'base'
    
    # base-devel
    'autoconf'
    'automake'
    'binutils'
    'bison'
    'fakeroot'
    'file'
    'findutils'
    'flex'
    'gawk'
    'gcc'
    'gettext'
    'grep'
    'groff'
    'gzip'
    'libtool'
    'm4'
    'make'
    'pacman'
    'patch'
    'pkgconf'
    'sed'
    'sudo'
    'texinfo'
    'which'

    # kernel
    'linux-zen'
    'linux-zen-headers'
    'linux-firmware'

    # Microcode
    'amd-ucode'

    # Editor
    'vim'

    # Filesystem
    'btrfs-progs'

    # Grub
    'grub'
    'grub-btrfs'
    'efibootmgr'
    'os-prober'
)
